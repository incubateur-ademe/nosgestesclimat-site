name: Cypress Tests

on: [push]

jobs:
  e2e:
    name: Testing e2e
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3.5.2
        with:
          submodules: recursive
      - name: Cypress run
        uses: cypress-io/github-action@v5.6.1
        with:
          install-command: yarn --immutable && yarn --cwd ./nosgestesclimat install --immutable
          build: yarn run model:compile
          start: yarn run server
          wait-on: 'http://localhost:8080'
